sudo: false
language: python
services:
  - docker
python:
  - "3.6"
install: pip install tox-travis
jobs:
  include:
    - stage: test
      name: Python Tox Tests
      script: tox
    - stage: test
      name: CSS Tests
      script: docker-compose run css-test
